import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ronanzenatti.aiionicapp',
  appName: 'AI Ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
